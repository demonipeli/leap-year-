#include<stdio.h>
#include<conio.h>

main ()
{
	int year;
	printf("Bir yil gir: ");
	scanf("%d",&year);
	
	if ( yil % 4 == 0 )
		printf("%d artik yil\n",year);
	else
		printf("%d artik yil degil\n",year);
		
getch ();
}

